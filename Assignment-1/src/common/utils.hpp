#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <vector>

std::string getInput();

std::vector<std::string> splitString(const std::string input, const std::string delimiter);

#endif // UTILS_H
